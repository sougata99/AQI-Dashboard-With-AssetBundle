# This is a Databricks asset bundle definition for air_quality_index_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: air_quality_index_bundle
  uuid: 3523a65d-b808-4382-9783-4823abe6fe5a

include:
  - resources/*.yml
  - resources/*/*.yml

# sync:
#   include:
#     - src/**/*.ipynb

variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use
  env:
    description: The environment (dev or prod)

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-67c4d502-2662.cloud.databricks.com
      root_path: /Workspace/Users/sougatapandit99@gmail.com/DEV/${bundle.name}/${bundle.target}
    variables:
      catalog: ${var.catalog}
      schema: ${var.schema}
      env: ${var.env}
    presets:
      name_prefix: dev[${workspace.current_user.short_name}]
      source_linked_deployment: false
  prod:
    mode: production
    workspace:
      host: https://dbc-67c4d502-2662.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/sougatapandit99@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/sougatapandit99@gmail.com/PROD/${bundle.name}/${bundle.target}
    variables:
      catalog: ${var.catalog}
      schema: ${var.schema}
      env: ${var.env}
    presets:
      name_prefix: prod[${workspace.current_user.short_name}]
      source_linked_deployment: true
    permissions:
      - user_name: sougatapandit99@gmail.com
        level: CAN_MANAGE